# HarnessTap Desktop UI Design

Visual language for the Tauri control plane. Product behavior lives in `SPEC.md` and feature specs; this file is the UI lockfile.

## Classifier

App UI (not marketing). Anti-slop: no purple/violet gradients, no 3-column feature grids, no icon-in-colored-circle rows, no celebratory emoji, no system-ui as the primary face font.

## Theme

Dark dense ops chrome. One accent (blue) for selection and primary actions. Status colors only for traffic-light state, always paired with a text label (`green` / `yellow` / `red`), never color alone.

## Typography

- UI: **IBM Plex Sans** (fallback: `"Segoe UI", sans-serif`)
- Mono (paths, versions, timestamps, content, counts): **IBM Plex Mono** (fallback: `ui-monospace, monospace`)
- Both faces ship inside the app (`@fontsource/ibm-plex-sans` 400/500/600 and `@fontsource/ibm-plex-mono` 400/500, latin and latin-ext subsets, imported in `main.tsx`). No runtime font CDN: the fallbacks are for missing glyphs only, never the first paint.
- Type scale is the `--text-*` tokens; no `font-size` literals outside `tokens.css`
- `--text-micro` 12px only for uppercase eyebrows and mono counts in pills
- Dense metadata (mono paths, timestamps, hints) `--text-meta` 13px with contrast ≥4.5:1
- Body (list rows, controls, form labels) `--text-body` 14px
- Field values and primary copy `--text-value` 16px sans; path / version / content / timestamps mono 13px
- Titles: workspace, pane, and dialog titles `--text-title` 18px; full-screen panel titles `--text-display` 22px

## Tokens

`:root` in `src/styles/tokens.css` is the source of truth. Do not invent extra palette roles. Do not reuse `--accent` or `--muted` as shadcn surface tokens. The tiers below are not palette roles. `styles.css` is an `@import` index; feature CSS lives under `src/styles/` and `scripts/check-css-tokens.ts` fails preflight on new hex, `rgba()`, `font-size`, or `z-index` literals outside `tokens.css` and `base.css` (the InUseMark Global fill `#22c55e` is the one allowed exception).

- Surfaces: `--bg`, `--surface`, `--surface-2`, `--border`
- Text: `--fg`, `--muted`
- Accent: `--accent`
- Status: `--green`, `--yellow`, `--red`
- Type: `--font`, `--mono`
- Radius: `--radius` (`0.25rem`; tight. Pills only for small filter chips and ResourceTypeTabs)
- Spacing (4px base): `--space-1` .. `--space-8`
- Type scale: `--text-micro` / `--text-meta` / `--text-body` / `--text-value` / `--text-title` / `--text-display`; `--leading-tight`, `--leading-body`, `--tracking-eyebrow`
- Surface states: `--surface-hover`, `--surface-selected`, `--surface-accent-tint`
- Border tiers: `--border-subtle`, `--border-strong`
- Status tints (banner and row backgrounds): `--tint-green`, `--tint-yellow`, `--tint-red`
- Focus: `--focus-ring`; `--focus-ring-on-accent` on filled accent controls
- Elevation: `--shadow-1` / `--shadow-2` / `--shadow-3` (shadcn `shadow-sm/md/lg` map to these), `--scrim` for dialog backdrops
- Z-index: `--z-sticky` < `--z-fab` < `--z-menu` < `--z-panel` < `--z-dialog` < `--z-popover` < `--z-toast` < `--z-tooltip`
- Motion: `--duration-instant` / `-fast` / `-base` / `-slow` / `-skeleton`; `--ease-out`, `--ease-in`, `--ease-in-out`
- Misc: `--opacity-disabled`, `--scrollbar-thumb`, `--scrollbar-thumb-hover`
- ResourceTypeTabs: presence-filtered pill/segmented type switcher (selected filled `--accent`, others muted outline). Hide empty types on Library, compose, Target preview type badges, and other inventories unless noted. Global/Project scope inventory shows canonical real types as wide icon+text pills, including zero-count types as disabled (`0 Permissions`, tooltip `No Permissions found`). Plugin pins share the **Plugins** tab (not a separate pin tab). Plugin refs and other alias types hide entirely when empty. Show **All** only when two or more types are present, or always on the Global/Project inventory. Pills always have icons. Library, Global/Project inventory, compose, Target preview type badges, and other type filters stay pills everywhere: icon, then count, then type text (`1 Skills`, `2 Plugins`); prefer one line; wrap over multiple lines when they do not fit; never collapse to icon-only. No compact circular type filters. The Add to profile modal collapses those pills to one row (types with count > 0). Trailing text `N more` stays at the end of that collapsed row and expands to wrapping icon, then count, then type text. Expanded `Less` sits right-aligned below the chip bank, with padding so it is not flush against the pills or the next section. Hide `N more` / `Less` when every present type already fits or there is nothing extra to show. Do not pass `density="compact"` or hide type text. The modal height follows content up to 70vh; the checklist scrolls. **All** is the total of present types. `aria-label` uses short pluralized copy: `12 resources`, `1 skill` / `3 skills`, same singular/plural for Plugins, MCPs, Subagents, Rules, Commands, Hooks, and Instructions. Hit target ≥32px. Overflow wraps. Icons use `TypeIcon` / `resourceTypeGlyph` / `resourceTypeTabGlyph`. Sparkles is **skill** only — never plugin, plugin ref, package, or instruction tabs or rows. Plugin packages use **Layers**; plugin refs use **Package**; instructions use **FileText**. All uses LayoutGrid. Short labels: Plugins, MCPs, Skills, Subagents, Rules, Commands, Hooks, Instructions, Permissions, Env vars, Model config, Plugin refs. Lean: one type control. ResourceTypeTabs is the only type filter on those surfaces. Never pair it with sidebar Type chips. Leftover TYPE chips are a bug. Global/Project inventory uses the same ResourceTypeTabs over Not in profile, Inactive, and Active. That inventory keeps All.
- Icon chrome: `--icon-action-size` (`32px` default squares). Discover header **Add marketplace** / **Connect catalog** use `--icon-action-size-lg` (`40px`) so they match in hit target and icon scale.

## Chrome

- Harness inventories are bordered sections / definition lists, not a card mosaic.
- Motion is functional, not decorative. Allowed: overlay enter/exit (opacity plus ≤8px translate or 0.98 scale, 120 to 180ms), pane swaps (crossfade 120ms), list row enter/exit and section expand/collapse (height plus opacity, ≤200ms), status color transitions (150ms), switch-step highlight (150ms), pressed state (80ms), skeleton shimmer (1.2s; static under reduced motion). Forbidden: bounces, springs longer than 300ms, parallax, looping attention animations except spinners and the skeleton shimmer. All motion reads `--duration-*` / `--ease-*` tokens and is disabled by `prefers-reduced-motion`.
- Motion recipes live in `src/styles/motion.css` only: `m-fade`, `m-rise`, `m-scale`, `m-panel`, `m-scrim` (each `-in` / `-out`), `m-status`, `m-skeleton`, `m-pressable`. Overlays keep exiting content mounted through `components/motion/Presence`; `Collapse` and `Crossfade` cover height and pane swaps. Feature CSS never defines its own keyframes.
- Interactive states: hover `--surface-hover`, pressed 80ms translate/brightness, focus `--focus-ring` (`--focus-ring-on-accent` on filled accent), disabled `--opacity-disabled`. Scrollbars are dark and thin.
- Labeled `btn primary` for dialog confirm actions (Create, Publish, Save, Update) together with an icon. Profiles rail **Apply** / **Re-apply** is a full-width accent `btn primary` with visible text and the icon to the right of the label. Library record primary actions that are not square icon chrome show a short visible label beside the icon: plugin **Apply**, resource **Sync** / **Write**, frozen **Restore**. Not in profile header **Add all** is a labeled `icon-action` with **Add all** to the left of the ListPlus icon; it is accent (`primary`) only when the rail is not already showing **Re-apply** (exactly one blue primary CTA in that chrome). Ghost **Add all** so Apply is the sole filled blue (Critiquito G4) is wontfix: **Add all** stays filled primary. Not in profile row **Add** is a labeled verb; drifted Active **View changes** is icon-only `FileDiff` with a tooltip. Other persistent page chrome — Clear, Remove, More, Show all, Install, Library/Discover header actions, Target preview install-gap plugin **Install**, and secondary/destructive record actions — is icon-only with a Radix tooltip plus `aria-label` (not a bare `title`). Header destinations show icon plus name. Header utility icons: RefreshCw **Refresh live status**; Settings; Ellipsis **More** — each icon-only with a Radix tooltip plus `aria-label`. Upload **Export setup**, Download **Import setup**, Account, and Update available are More menu items at every width. Discover header **Add marketplace** / **Connect catalog** the same. Lucide Sparkles is the **skill** type glyph only (`TypeIcon` / create-type picker / `resourceTypeGlyph("skill")` in `type-glyph.ts`) — never instruction, plugin, plugin package, plugin ref, agent, Not staged, Install gaps, or generic trailing chrome. Agents use **Bot**. Plugin packages use the plugin type glyph (`Layers`). Not staged and Install gaps use a neutral status glyph (`CircleDashed` / `CircleAlert`) plus tooltip or text, not Sparkles. Library list rows and profile package / not-staged identity rows do not trail related-harness marks (Claude’s asterisk mark is not a type glyph); harness marks stay on the hover card. Library list rows always reserve a leading InUseMark column (shape, not color-only; Radix tooltip on used states; unused is a same-width spacer). Pencil stays profile **Edit**; the Settings gear stays Settings.
- Action clusters: flex, gap ≥ `0.4rem`, never flush, never `space-between` siblings for two or three related buttons. File change trailing chrome is the same: kind chip then icon actions as separate flex items, never flush against trailing copy.
- The More menu holds an **Update available** item (arrow-up icon plus a red badge); the More trigger shows the same red badge while an update exists. The badge is paired with the accessible name **Update available** (current → newer). Click opens a centered dialog with release notes, a GitHub release link (text link, not a chrome button), and **Update** (icon plus label; downloads the matching GitHub installer for this os/arch). The item is omitted when Desktop is current.
- Overlays register in one layer stack: Esc and focus trap apply to the top layer only; focus returns to the opener. Popovers (project picker) join the stack for Esc but do not trap Tab. The telemetry consent modal joins the stack with close disabled so it traps focus without dismissing.
- Overlays: full-screen panels (not side drawers) for settings, create/edit, import, apply, browse, migrate, account, stash, and history. Resource inspect is a centered, viewport-capped dialog (scrollable body, height follows content). File apply diffs are the same centered, viewport-capped dialog (not pinned to a pane corner). The command palette (`⌘/Ctrl+K`) and the keyboard cheat sheet (`?`) are centered dialogs. Back (top left) and Esc leave the panel or dialog. Confirm dialogs only for destructive or discard. Report dialogs for Doctor. The Library create-resource type picker is a centered dialog. Library record detail is a full-panel document in the workspace, not a modal. Cloud browse overlay is gone.
- Settings: labeled tabs **Project | Advanced**. One tab visible at a time. No footer Save / Cancel: every tab saves inline. Back and Esc confirm discard when the Project `apm.yml` editor or the **Harness override** draft is dirty. Project tab: project directory picker (recent + Browse), raw `apm.yml` editor with a line-number gutter, mono `--text-meta`, and inline error markers when validator messages include line numbers (save runs existing config validate and refuses on errors), icon-only Open config as the external-editor escape hatch (tooltip **Open config**), profile definition list, validation/load errors, then **Harness override** (override switch, project main harness, alias list, materialization, inline labeled **Save** / **Cancel**). Harness alias rows are left-aligned `[icon] [name] …… [checkbox]`, 40px tall; the list height follows content up to 320px. The override save re-reads the saved global selection and sends it back unchanged with the project block; the global selection itself is edited on the **Harnesses** destination, never in Settings. Marketplaces and catalogs are managed in Discover, not Settings. Advanced includes labeled **Check for updates** and **Reset telemetry choice**.

## Layout

Window minimum size is 960×600. Header grid is brand, destinations, scope, project cluster (flex 1, min 180px), then utilities. Brand text never clips. Destinations size to their icon-plus-label (or icon-only) controls and do not shrink or clip into the Scope divider; leftover width goes to the project cluster. Header utilities are Refresh, Settings, and More (Ellipsis) at every width; Export, Import, Account, and Update always live in the More menu, and More shows the red update badge when an update exists. Below 960px, destinations are icon-only with tooltips (hit ≥32px) and the Scope label hides.

First connect shows a centered splash (brand, indeterminate bar, Starting the agent…). Later disconnects keep the last-good shell dimmed with a Reconnecting banner that offers Retry after 5s. First loads use skeletons (rail, inventory, Library, Discover, Environments). A failed switch step is red with a failed suffix, not the in-progress highlight.

Header destinations (equal-weight primary nav): **Library | Discover | Environments | Harnesses**. **Global | Project** is not a destination peer: it is a quieter **Scope** segmented switch after the destinations (visible **Scope** label). Scope stays ambient for the profiles rail and apply when an inventory is selected (Global/Project remain highlighted from `view`, not only when the live-state workspace is open). Project picker, Install, and History appear only when Project is the selected scope and the live-state workspace is showing. No header Plugins tab. No header Apply-plugin control. No live-state header Apply icon (sidebar rail **Apply** / **Re-apply** is the only profile-apply path).

| Workspace | Shell |
| --- | --- |
| Global / Project | Profiles rail (~220px) + live-state `main`. Below ~720px, stack rail above the pane. No multi-panel cockpit. Preview changes swaps that main content to Target preview (not a permanent third column). |
| Library | Filter sidebar + one main pane (list **or** detail). No profile rail. No Items/Packages tabs. |
| Discover | Source sidebar + one main pane (list **or** plugin-tree **or** preview). No profile rail. |
| Environments | Full-width list + detail. No profile rail. |
| Harnesses | Harness sidebar + one main pane (type groups **or** resource detail). No profile rail. |

Landmarks: `nav` (profiles rail, scope layout only), `main` (live, library, discover, environments, or harnesses).

## Interaction locks

Durable contracts only. Screen recipes belong in the feature spec that introduced them.

**Lean UI — one path per action.** Don’t offer two controls or two status phrasings for the same job unless the second clearly earns it (different outcome, different audience, or recovery). Desktop: no dual Apply; don’t pair tint+counts with redundant status words that repeat the same signal. Quiet icon chrome that mutates disk still confirms in a modal (Re-apply).

**Voice**

- Short, human, tool-like. Tooltips and action blurbs, not lectures.
- No em dashes in Desktop system/UI strings (tooltips, empty states, helpers, status, confirms). Prefer a period, comma, parentheses, or a short second sentence.
- Not robot, not condescending (no “Simply…” / “Just…” / “please carefully…”).
- Prefer verbs users do: `Refresh live status`, `Export setup`, `Sign in to Cloud`.
- One idea per string. A second clause often means redesign, not a longer tooltip. Detail belongs in a confirm or modal.
- Bad: `Refresh live status and rescan tracked directories — this may take a moment.` Good: `Refresh live status`.
- Bad: `Install this project's apm.yml (ht install). Not a profile switch.` Good: `Install project config`.

**Feedback**

- Success and info results are toasts, bottom-right above the FAB (`--z-toast`), at most three visible, success auto-dismisses after 4s. Error toasts stay until closed. No header success flash and no inline "Saved" copy for the same result.
- Errors that block a pane (connection, live status, profiles, add resource, switch) are inline `Banner`s with the same **Retry** / **Dismiss** controls everywhere. Held values stay on screen while a refetch runs; nothing flips to empty or Loading after the first load.

**Field identity**

- Non-obvious user-authored text (especially profile **description**) has a leading Lucide **TextQuote** whose tooltip and `aria-label` are **Description**. Profile **title** stays icon-free. Library Description field rows use the same **TextQuote** glyph.

**Header destinations**

- Re-clicking an already-active header destination (Library, Discover, Environments, Harnesses) returns that view to its entrypoint and clears its filters. Clicking a different destination only switches. Scope (Global | Project) is a separate control: it does not sit in the destination peer row. Re-clicking the current scope while live-state is open clears the profile rail search and closes edit-profile; the selected profile stays. Project re-click does not reopen the directory picker.
- Back icon to the left of the panel title (Library, Discover, Environments, Harnesses, Profiles). Back returns to the previous screen: nested pane first (Library detail / history, Discover preview / plugin-tree, Harnesses resource detail, or edit-profile), then the previous header destination. The control is hidden when there is nowhere to go. Library, Discover, Environments, and Harnesses **list** entrypoints hide Back even if another destination is in history. Switch workspaces from the header; Back appears only for a nested pane.
- Library re-click applies default filters and returns to the list via the same path as a sidebar filter change (`applyFilterChange`).
- Discover re-click clears the search query, turns off **Show in library**, checks all sources, and returns to the list. Add/edit panels stay if open. Sidebar **Clear filters** uses the same filter defaults (empty search, **Show in library** off, every source checkbox checked).
- Environments re-click clears the name filter and deselects. Create/edit full-screen panel stays if open.
- Harnesses re-click selects the main harness, clears the resource filter and type tab, closes resource detail, exits edit mode, and closes any Add / Detect / Remove dialog.
- Global and Project re-click clear the profile rail search and close edit-profile; the selected profile stays. Project re-click does not reopen the directory picker.

**Keyboard**

- `⌘/Ctrl+K` opens the command palette (destinations, profiles, settings, account, actions on the current screen, recent projects). `⌘/Ctrl+1..4` jump to Library / Discover / Environments / Harnesses. `[` / `]` switch scope. `/` focuses the current workspace filter. `?` opens the shortcut cheat sheet. `Esc` dismisses the top overlay layer, else Back.
- Shortcuts with a modifier work while typing. `[`, `]`, `/`, and `?` do not.
- Row lists (Library, inventory, Discover, Environments, command palette) share the listbox recipe: `role="listbox"` / `option`, Arrow/Home/End, Enter activates. Typeahead is first-letter prefix, wrapping once.

**Empty states**

- Shared recipe (`EmptyState`): heading at `--text-title`, one sentence of why, one action. Centered, max-width 320px. Compact pickers and comboboxes use the same miss title without the full recipe.
- Search-miss copy is `No results for "<query>"` (or `No results` when the query is empty) with **Clear search** / **Clear filters** / **Clear filter** when that control exists. Never "You're caught up" for a query miss.

**Scope (Global / Project)**

- Clicking a profile selects it. Re-clicking the selected profile keeps it selected (no toggle-deselect). **Apply** commits. No “Both” view. Apply applies to the current view only. On app open (and whenever the rail has no intentional user pick yet), the selected profile is the currently applied/active profile for that view (`status.active_profile`) when it is in the rail; otherwise the first visible profile. There is always a selected profile when the rail is non-empty (fallback: active, then first). The same active profile is pinned first in the Profiles rail for that view (ahead of the desktop-only localStorage order). Global and Project use one active pointer; each view only pins/selects it when that profile is enabled in that scope.
- Profiles enabled in home appear in Global; profiles in project config appear in Project; enabled in both appear in both. The init profile is named **global default** and stays Global-only. Desktop project bootstrap seeds a **project default** from that repo’s on-disk resources and does not enable the global profile in the project. Auto-seeded project defaults are project-scoped only.
- Global and Project share one live-state component (`LiveStatePanel`). Header left: profile name and muted description. One status sentence under the name: `Selected · not applied` or `Active · applied`. Drifted file counts stay on the rail Apply helper and Active row **View changes**, not a second header sentence. The `active` pill next to the title is removed; the rail keeps its `active` badge. Header right is icon-only (Radix tooltip + `aria-label`): **Edit** (`Pencil`, becomes **Done** in edit mode), **Overwrite with current setup** (`HardDriveUpload`, next to Edit), **Preview changes** (`FileDiff`, not Eye), cut, and remove. Overwrite confirms, then adds, updates, and removes profile resources so the snapshot matches disk. **Preview changes** swaps main content to Target preview. Exit preview by toggling the icon off or the preview header **Close preview** (`X`). It is not a permanent third column and not a collapsible side panel. No section-level Apply. Rail Apply / Re-apply is the sole profile-apply CTA (sidebar only). Under Apply: `N changes · Preview` when the selected profile is not active and the preview has a delta; `Up to date` when applied and clean. Apply is disabled only when there is nothing to do.
- Scope inventory (shared Global/Project): one full-span search (filters type-tab counts, attention dots, and every section). ResourceTypeTabs are wide pills (icon, then count, then type text). Zero-count real types stay as disabled pills (`0 Permissions`, tooltip `No Permissions found`). Plugin pins share the **Plugins** tab. Plugin refs hide when empty. Type tabs show an amber attention dot (not a tinted pill) on the **top-right corner of the chip** (not over the type icon) when the current search has Not in profile or Inactive rows of that type; **All** gets the same dot if any type needs review. Tooltip is `1 MCP to add · M inactive` (singular type noun when the add count is 1; `2 Skills to add` when N ≠ 1). Same pattern for other types. Disk-diff on Active stays row-level only. Sections in order: **Not in profile → Inactive → Active**. Each section is collapsible (header click toggles; `aria-expanded`; caret down when expanded, right when collapsed). Default: all panels expanded. Quiet section chrome: small label, count as a pill badge to the right of the title, and a muted status glyph (`CircleDashed` / `CirclePause` / `CircleCheck`). Inventory row type icons stay vertically centered with the row title. Do not list the selected profile as a plugin or resource row (name, id, or package self-ref). Inventory lists **direct memberships only** (plugin packages, pins, and profile-level resources). Nested plugin contents stay on plugin details Composition. They must not appear as inventory rows or inflate type-tab counts. Directly attached non-plugin resources still show. Inventory rows do not show an `in {this profile}` subtitle. Nested `in {plugin}` stays on plugin details Composition. Clicking a plugin package membership row opens Library plugin details by package name (same destination as Library), not the library-resource inspect modal; plugin ULIDs are not resource ids. **Not in profile** is on the harness and not in this profile config. Row **Add** (`Plus`); section **Add all** (respects search+tab). Labeled **Add all** (text left of ListPlus) is accent (`primary`) only when the rail is not already showing **Re-apply**. Ghost **Add all** so Apply is the sole filled blue (Critiquito G4) is wontfix: **Add all** stays filled primary. **Inactive** is in the profile and off the harness. Row **Activate** (`Power`) only when the selected profile is active; otherwise the section header says `Applies when you Apply this profile`. Section **Activate all** (filtered) follows the same rule. **Active** is in the profile and on. If the live copy differs, the row is amber with trailing **View changes** (`FileDiff`) that opens the centered diff modal. Pencil enters edit mode (`Trash2` on Inactive and Active, quiet confirm). **Done** exits. Not in profile stays **Add** only. Inventory rows reserve a trash-width trailing slot so names do not jump when entering or exiting edit (placeholder when trash is hidden). In edit mode the trash is red (`profile-remove-action`) and always the rightmost icon (after View changes). Rows are `[type icon] [name] [subtitle?] …… [action] [trash]` at 40px. Drop the leading status glyph except drifted Active rows (amber `m-status`). Whole row is a button; names are not underlined. A `+` FAB is always on the inventory (tooltip **Add to profile**), overlaid on the bottom-right of the visible pane over scrolling content (not in document flow). Scroll area padding-bottom keeps the last row clear of the FAB. It opens a modal titled **Add to profile** with header **Create** (Library create flow: `ResourceTypeModal` then the create form) on the same header row as the title (title left; Create and close right), search, ResourceTypeTabs collapsed to one row by default (icon, then count, then type text; types with count > 0; trailing text `N more` at the end of the collapsed row; expanded `Less` right-aligned below the wrapping pills, with padding so it is not flush against the chips or the next section; never icon-only), a scrollable checklist (`checkbox | type icon | name` on one row) whose height follows content up to 70vh, and footer **Cancel** / **Add** (Add needs at least one selected row). The selected profile’s own package is not listed. First-run with no profiles: main pane heading **Create your first profile**, one sentence, and a primary **Create profile** (Compose preselected). After create, that profile is selected and the Apply helper shows. Project scope with no `apm.yml` shows the bootstrap banner in main by default. After project bootstrap, if the seeded profile is not applied, the Inactive header says `Found on disk. Apply to track these with this profile.`
- Target preview (Preview changes content swap, same `LiveStatePanel`): header is **Target preview** plus icon-only **Close preview** (no chevron). No second Apply. Inner chrome starts with **resource-type** badges: one TypeIcon/glyph per type present in the pending apply delta (including File changes paths that map to typed resources such as instruction files `.claude/CLAUDE.md` / `.codex/AGENTS.md`, skills, and other typed paths — not only stack rows with resource metadata), same map as ResourceTypeTabs (Sparkles skill, Bot agent, Layers plugin, Package plugin ref, FileText instruction). Pills: icon, then count, then type text (`1 Skills`); wrap; do not collapse to icon-only. Hide empty types. Not +/−/edit at the Target preview top. File changes header holds **change-kind** badges (green **+**, red **−**, yellow pencil; hide zeros; never **+0** / **−0** / edit **0**) as filters, not stacked type glyphs (no **N would change · N managed**). The two rows mean different things: Target preview top is resource types in the apply delta; File changes header is add/delete/modify. Managed count is a File changes title tooltip only (`N managed`), never inline header prose. Stack changes has no info icon. Stack and file changes omit material already deployed by an inherited host plugin; host-native plugin MCP (for example Cursor Slack) is present, not an install gap. Quiet empty (no stack, file, or install-gap rows) drops inner chrome (diff badges, empty Stack changes, empty File changes) and centers a large muted checkmark with **no changes** under it (deactivated grey, not accent). Stack and file panels appear only when they have rows. No extra Drift word and not amber-only. Install gaps (in profile) group as Plugins, Value diffs, Not installed, then Outside profile (plugins and value diffs expanded first). MCP that is in the profile and not currently installed uses **+**; MCP whose live config differs from the expected install uses **!**; missing plugins keep **!**. Missing-plugin rows use icon-only **Install** (`PackagePlus`, tooltip and accessible name **Install {plugin}**); click still runs `sync-install`. Other gap kinds use a quiet verb (**Apply to install** / **Apply to update**), not Sync. Install gaps keep a one-line definition plus an info tooltip. Stack changes keeps the one-liner and has no info icon. Truncated lists use icon-only **More** and **Show all**. Snapshot-only MCP from a whole-file `mcp.json` import is omitted from stack removals when that server is gone from the live file. Shared host configs such as Claude `.claude/settings.json` preview as edits (merged profile keys), never whole-file deletions. Skill apply diffs merge extra live frontmatter and body when the profile snapshot is a subset of the live file. File changes lists every would-change path (kind badges live in the File changes header; the path filter stays in the body). Rows are `[path] … [kind chip] [icon actions]`: chips match filter-chip padding and sit in a flex trailing cluster (gap ≥ `0.4rem`) beside icons, never flush. Missing-file (+) chips tooltip **Will be written when you Apply**. No per-row Apply copy. Rail Apply writes. Target preview does not use the Not in profile amber tint.
- Switching: yellow overlay strip at the top of the live pane (ordered steps, cancel disabled during apply). Inventory stays mounted and read-only underneath. On success the strip turns green briefly then collapses. After apply, switch, or Project Install, a yellow pending-approvals strip lists parked executable refs when the trust gate is on.
- **Remove profile** is an icon-only trash control (accessible name **Remove profile**; tooltip **Remove profile**). Disk files stay until you apply another profile (confirm dialog). It lives in the edit-profile header next to Done, and as a trailing live-toolbar control (far from the Profiles rail). Never in the Profiles rail next to create **+**. Create vs remove must not be confused: rail **+** is accent **Create a new profile**; trash is destructive-tinted (`profile-remove-action`) and never a peer of that cluster. Confirm dialog keeps the delete-plugin checkbox. Add paths are inventory **Add** / **Add all** and the FAB only (no rail ListPlus not-staged control).
- Name and description are the first fields of the full-screen edit pane (not behind Metadata). The live-state header pencil is inventory **edit mode**, not that pane. The edit header subtitle is last-edit time (`Last edit 1 day ago (2026/09/07 14:32:05)`: relative phrase plus local `YYYY/MM/DD HH:MM:SS`), not autosave copy. It refreshes after each saved change. Composition stays below and is the membership editor for the current profile: one full-width filter over plugin packages, plugin refs, and material library items, then ResourceTypeTabs and a flat filtered list. Checkboxes add or remove the current profile; checked rows are already linked. Clicking a composition resource row (not its checkbox) opens the resource inspect dialog. There is no separate Plugins checkbox column.
- Profile rail rows can be dragged to reorder when the filter is empty. A **GripVertical** handle appears on hover/focus. Keyboard `Alt+↑/↓` on a focused row reorders. The drop indicator is a 2px accent line between rows. Profile rail order is a desktop-only localStorage preference, separate for Global and Project. The applied/active profile for the current view stays first in the list. The Profiles header action cluster is stash, restore, **Publish**, then **create** (accent icon-only `+`, tooltip **Create a new profile**). Publish opens a centered catalog-picker dialog (checkboxes, select-all, remembered last selection). With no registered catalog the list shows **no catalog registered** and Publish is disabled. Default environment Clear is an icon to the left of the dropdown; the dropdown includes **None** and **Create a new environment** (+). Profiles filter **Clear** is icon-only (X, tooltip **Clear**) and only shows when a filter is active. Rail **Apply** / **Re-apply** is a full-width labeled accent button with Check / RotateCw to the right of the text. Long home paths in the Project picker truncate with `~`.

**Apply vs Sync**

- Rail Apply = profile switch / re-apply (full-width labeled accent button; icon to the right of **Apply** / **Re-apply**). This is the sole profile-apply CTA. Live-state header has no Apply/check icon.
- Project **Install** = `ht install` (`POST /v1/apply` with `plugins: []`, project scope). Icon-only with tooltip **Install project config**. Never labeled Sync or Switch. No `--global` install.
- Library plugin Apply = `ht apply` (plugin graph). Labeled accent **Apply** (icon + word). Does not use profile switch. Global apply of a profile-tagged package still records the active profile (`ht apply --global`).
- Library **Update** = `plugin update` / `ht plugin update`. Refreshes the **whole plugin package** in the library from the marketplace, catalog, or git source it came from. Not a single skill or file (that is **Sync** on the skill). Does not install into a project. Never labeled Sync.
- Library Sync = `resource sync`. Never means Apply. **Sync** checks whether this library item differs from the copy in the marketplace, catalog, or place you got it from; it does not change files yet and does not install into a project. **Write** saves that newer copy over this item’s library files only (not the whole plugin package, not a project).
- Sync on a library row is `resource sync`.

**Library**

- Client-merged list of resources and plugin packages. Plugin packages are type **plugin**; composition-ref rows (`resources.type = plugin`) are type **plugin ref**. ResourceTypeTabs filters a flat list; plugin vs plugin ref stay separate tab keys. Do not pass `density="compact"`; Library uses `overflow="collapse"` and measures `N more` with the real hidden count.
- Main pane is list XOR full-panel detail. Back (top left) and Esc return to the list. Esc while editing a field cancels that edit only (`cancel-field` calls the detail cancel callback). Sidebar filter changes return to the list (blocked while a confirm is open or an action is in flight).
- `LibraryDetailChrome` Back defaults on for the standalone inspect dialog. Library workspace detail passes `showBack={false}` so the shell Back is the only one.
- Reload keeps the current rows and sets `refreshing`; a spinner next to the Library title marks the refresh. New rows enter with `m-fade-in`. First load with no rows still shows Loading.
- The filtered list is virtualized (`@tanstack/react-virtual`): 44px rows, 56px with a subtitle. `role="listbox"` / `role="option"`, roving tabindex, ↑/↓/Home/End, Enter opens, typeahead (500ms). The whole row opens detail. After Back, the last opened row is `aria-current` on the selected surface.
- Sidebar search uses `useDeferredValue`; filter/sort is memoized. Updated is a Select (All time / 1d / 7d / 30d / 90d / Custom) so labels never wrap at 220px. Section labels use the eyebrow recipe. Filter-empty copy is `No results for "<query>"` (or `No results`) plus **Clear filters**.
- Rows whose name differs only by `@<profile>` group under the base name when that base exists: indented `.resource-row-scoped`, mono `@<profile>` chip, hover `Scoped copy in <profile>`. Global-scoped rows (empty namespace) show a parallel mono `@global` chip so Global vs Project is symmetric. `AGENTS.md` / `agents-instructions` list and detail titles use **Agent instructions (AGENTS.md)**; storage ids stay `agents-instructions`.
- Authored plugin detail **History** opens a version list in the main pane (not a drawer). Frozen inspect is the same detail document, read-only. Back/Esc: frozen → version list → working head → library list.
- Restore copies a frozen snapshot onto the working head and marks it dirty. It does not Apply. Frozen restore is a labeled accent **Restore**.
- Filters: name search, updated as a Select, namespace as a searchable select, origin as a radio list. No sidebar Type chips. Type uses shared **ResourceTypeTabs** above a flat list (single-select; no section headers). One type control only: do not leave TYPE chips as a twin of the tabs. Pills always include the type icon. Pills: icon, then count, then type text (`1 Skills`). Tab order: All, Plugins, MCPs, Skills, Subagents, Rules, Commands, Hooks, Instructions, then rarer (Permissions, Env vars, Model config, Plugin refs). Origin groups `manual` and `local_snapshot` as **Local**; `marketplace_link` as **Marketplace**. Hover and detail origin copy is `Local` or `Local (/absolute/path)` when `origin_ref` is a path; marketplace is `Marketplace` or `Marketplace (ref)`. Storage `origin_kind` is unchanged. Filter placeholder is **Filter by name** (typed `type:name` queries still work). **Clear filters** restores every facet to its default (All / empty), including the type tab. Filter-empty uses `No results for "<query>"` (or `No results`) plus **Clear filters**.
- Detail: title is the name (`Agent instructions (AGENTS.md)` when that is the shared AGENTS.md instruction). Fields are icon + visible field name + value; icon tooltip repeats the field name. Field rows and the title show a Pencil icon-action on hover/focus (tooltip **Edit**). Click the value or press Enter to edit; double-click still works. Multiline content has **Save** / **Cancel**; single-line commits on Enter/blur and cancels on Esc. Default environment is only the field-row combobox. Updated is locale absolute date with relative time in parentheses. Plugin-type rows (`resources.type = plugin`) omit Description and Namespace. Origin is Local / Marketplace only (no `origin_ref`). Marketplace URL shows for Marketplace origin when the registry has a URL. Path is the install directory. Content lists nested files under that directory (relative paths), each with the same Path actions (Copy path, Reveal in Finder, open-in-editor). Plugin-ref empty Content is `Nothing loaded yet.` plus an inline labeled **Sync**. Skill packages and other `SKILL.md` / `plugin.json` packages use the same nested file Content (Path is the package directory). Other material Content is a 15-line code block. Path shows the full path except skill packages and other `SKILL.md` / `plugin.json` entry files, which show the top-level package directory. Clicking the folder icon or the path reveals it in Finder on macOS (file manager elsewhere) via `POST /v1/open-path` with `reveal: true` (agent sidecar, not a fake webview open). Path actions are Copy path, Reveal in Finder, and (material files) open-in-editor. Detail field rows use even vertical gap with no per-row vertical margin. MCP server Content shows that server’s config from the live `mcp.json` (or metadata), not an empty placeholder.
- Header cluster (right of the Library title): icon-only **Create resource** (accent), **Import**, **Tracked directories**, **Update all**, each with a hover tooltip of that label. Create resource opens the centered type-picker dialog. That dialog’s header has a primary labeled **Import** (icon + text) that opens a GitHub URL paste field (`https://github.com/owner/repo`, `owner/repo`, or `gh:owner/repo`); a root `plugin.json` imports as a git-origin plugin package. The create-from-scratch type list stays and uses `TypeIcon` / `resourceTypeGlyph` (plugin **Layers**). Close is `IconActionButton`. After picking a type, a full-screen single-page form per type has Cancel/Create in the footer; canceling dirty edits confirms discard. The form uses `ui/input`, `ui/textarea`, `ui/select`, and `ui/checkbox`; helper text is the schema description. Success toasts `Created <name>` and lands on detail. Material forms offer an optional add-to-current-profile-and-apply checkbox. Plugin create **Compose from library** reuses the profile membership picker (filter + ResourceTypeTabs + flat list, including plugin packages and plugin refs). **Update all** is disabled when origin check found no outdated plugin-package heads or an update is in flight. Confirm: “Update N plugins from origin?”
- Tracked directories auto-opens once per install (`ht.desktop.trackedDirsIntroSeen` in localStorage). It does not reopen on later Library visits.
- Plugin-package list rows show a yellow **Update available** badge (color + text, never color alone) when origin check is outdated. Not on authored, frozen, or resource rows.
- Plugin detail **Composition** lists that plugin’s members only (not the global library). ResourceTypeTabs filter that membership; types with count > 0 stay selectable (empty/disabled styling only when count is 0). An always-on `+` FAB (tooltip **Add to plugin**) sits at the bottom-right of the composition pane over the list, same overlay as the Global/Project inventory FAB. It opens **Add to plugin** (search, same collapsed type pills as Add to profile, checklist of library items not already in the plugin). The shared picker keeps a fixed-height, scrollable list. Create-plugin **Compose from library** and edit-profile Composition stay full-library membership pickers. Composition toggles are optimistic and roll back with a toast that offers Retry.
- Library list rows always reserve a leading **InUseMark** column (~10px glyph, 20px column / tooltip hit) from profile composition and Global/Project enablement. Unused: empty spacer of the same width (no tooltip). Project only: Lucide `Circle` (stroke 1.5, fill none, muted). Global only: filled 10px disc in success green `#22C55E` (optional 1px darker edge; no outer ring). Both: ~6px filled disc centered in a 10px empty ring (same muted stroke as project; relative ring + absolute fill). Tooltips: **In 1 project** / **In N projects**; **On Global**; **On Global · in N projects**. Shape carries meaning; green is the Global fill only. No Sparkles / Check / Globe / CircleDot. No unused-only filter yet. Shared `ResourceRow` chrome grows with wrapped descriptions; `padding-block` is `--space-3` so content stays off the separator. Virtual lists measure that height so the next row does not sit on the divider. Library uses `LibraryResourceList` with `measureElement`.
- Plugin detail actions for upstream/catalog working heads: labeled **Apply** (icon + Apply), then icon-only **Update**, Fork, Doctor, Delete (tooltips keep those labels). Authored heads: labeled **Apply**, then icon-only History, Cut, Doctor, Delete. Update has no extra confirm; `busy` spins Update. Only the in-flight control and destructive peers lock. Back stays enabled unless a confirm is open.
- Resource detail (skill, MCP, plugin ref, and other library material): labeled **Sync** previews `resource sync` (does not change files yet); labeled **Write** saves the newer copy over this item’s library files only. If a skill lives inside a plugin package, only that skill’s library entry is updated — not the whole plugin, and not a project. Secondary **Delete** stays icon-only.
- Delete confirm: list profiles and plugins that still attach this selector. Offer **Remove from active profile** as a tertiary action when it is enabled there. Primary action is **Delete from library + disk**; secondary is **Delete from library**. Load a delete plan before opening the dialog; group locations by global / project / source; show **Protected** blockers with the full protected path plus Copy / Reveal. Disable only the disk-inclusive action when hard blockers exist (shared aggregate files, paths outside managed roots, missing/unreadable paths), and show a visible human reason next to that control (not tooltip-only). Inspect does not show a yellow delete-plan banner by default; an Info icon-action next to Delete (tooltip **Disk delete blocked**) carries the blocker reason, which also appears inside the confirm. When the on-disk copy was edited outside the library (hash mismatch), keep disk delete enabled and require a checkbox confirm: **Disk copy differs from library. Delete anyway?** Plugin names in attachers can open the owning plugin package in Library.

**Discover**

- Source sidebar + one main pane (list XOR plugin-tree XOR preview). No profile rail. Marketplaces and catalogs are managed in Discover, not Settings.
- The sidebar lists HarnessTap-registered marketplaces and host-configured Claude Code marketplaces from `~/.claude/plugins/known_marketplaces.json`. Host-only rows are not editable or removable in Discover.
- The source sidebar groups checkboxes under Local, Marketplaces, and Cloud. Empty sections are omitted. A top-level **All sources** checkbox selects or clears every source; mixed selection is indeterminate. When every source is included, only **All sources** appears checked. Children are implied, not a second row of checks: they render unchecked at full opacity (not faded). Choosing a child starts a specific selection. An opt-in **Show in library** checkbox (off by default) reveals hits already in the library; the default list hides those rows so Discover surfaces items to add. **Clear filters** restores empty search, **Show in library** off, and every source checkbox checked.
- Discover re-click clears the search query, turns off **Show in library**, checks all sources, and returns to the list. Back/Esc: preview → plugin-tree → list (standalone preview skips the tree). Exactly one Back control (workspace header). Esc while a confirm is open dismisses the confirm only.
- Header cluster (right of the Discover title): icon-only **Add marketplace** (accent) and **Connect catalog**, with those tooltips. Both are `--icon-action-size-lg` squares (not text CTAs). Add marketplace is the only accent control in this cluster.
- **Add marketplace**, **Connect catalog**, and **Pin to plugin** are full-screen panels (not centered create-profile dialogs). Accessible names do not say "drawer".
- Record actions: labeled accent **Add to Library** (Download) for anything not yet in the library (Cloud pull and marketplace package install). Secondary icon-only: **Pin to plugin** (tooltip **Link into an authored plugin**), **Open in Library** when present. Attach is folded into Pin (same picker, attach wording in the picker body). Helper under the tree/preview cluster: `Add copies it into your Library. Pin links it into one of your plugins.` No Update button on Discover.
- Pin picker always offers **Create plugin** as a secondary action, even when authored plugins exist. It stays open on failure and shows the error inline.
- List rows use `ResourceRowRoot` (same hover/selected/focus recipe as Library) with **InUseMark** when the hit is in the library. Trailing Add to Library and Pin sit vertically centered in the row (type icon, title, and description). List and nested panes crossfade.
- In-library marketplace and catalog hits show a yellow **Update available** badge (color + text) next to In library when origin check is outdated. Open in Library is the apply path. Pin stays. Those rows appear only when **Show in library** is on. A failed origin check uses its own banner, not the Add/Pin error.
- Search: `Searching…` only on the first fetch per source; later refetches keep the list and show a small sidebar spinner. Query miss: heading **No results for "<query>"** plus **Clear search**. True empty with an empty query and **Show in library** off (`discover-empty`): **You're caught up** plus hint **Nothing left to discover. Turn on Show in library.** Do not flash that copy before the first source fetch finishes.
- Cloud catalog discovery lives in Discover. The Cloud header control is account-only. Waiting for approval shows a spinner next to **Waiting for approval…**. Sign-in copy talks about plugins and catalogs, not profiles.
- Preview field rows reuse the LibraryFieldRow grid so label, value, and icon align. Hide **Contained files** when the list is empty. Cloud sign-in banners use a labeled **Sign in** button.

**Environments**

- Globally active environment is a list-row `active` badge, not a header status marker.
- Name filter lives in the list sidebar. **Clear filter** clears that query. Sidebar inventory is values and secrets only. Plugins that default to this environment appear in detail as “Plugins referencing this environment” and open that plugin’s Library detail.
- List first load uses skeleton rows. Selecting another environment shows a skeleton immediately (never the previous environment's values). A same-environment refresh may dim the current detail.
- Create is accent (`primary`). The list entrypoint hides Back. Create includes key/value rows and a Secrets hint; after create, land on detail.
- Detail: keyed definition list + harness blocks. Apply / edit / delete / **Copy all** are icon-actions in the detail header. Apply (`environment use`) only when process env drifted from this environment. Values whose keys match `*_KEY`, `*TOKEN*`, or `*SECRET*` have a mask toggle.

**Harnesses**

- The sidebar lists the saved global selection (main first, then aliases); "set up" means saved, not found on disk. Disk presence is an annotation on the row (`not on disk`, `shared paths only`), never a health badge. `main` is a list-row badge. Empty: **No harnesses set up.** with primary **Detect harnesses**. The list column sizes to the rows (`max-content`, min 11rem, max 16rem), not the 220px Library filter track, so leftover width stays with the inventory. Selected/hover chrome covers the whole row, including the reserved trash slot.
- Sidebar action cluster (gap ≥ `0.4rem`): accent Plus **Add harness**, ScanSearch **Detect harnesses**, Pencil **Edit** / Check **Done**. Trash appears on rows only while editing, in a reserved trailing slot, and is disabled on the last harness (tooltip **Keep at least one harness.**). Plus never sits beside a row trash.
- Add opens a centered single-pick dialog of unconfigured harnesses (detected first, then name). Row subtitle lists supported resource types (Skills, MCPs, …), not disk presence or path-based mirroring. Detect refetches and proposes adds (detected, not saved; checked) and removes (saved, absent; unchecked) in one `ConfirmDialog`; shared-path-only harnesses are never proposed for removal. No changes toasts **Harnesses are in sync**. Remove is a destructive confirm whose body says the harness leaves the list and **Files on disk stay.**, naming the promoted main when the main is removed. Add, Detect, and Remove never stack.
- Main pane: name, `main` badge, summary line, icon-only Star **Make main harness** on aliases. Then **Filter resources** and wide ResourceTypeTabs (counts follow the search). Filter resources is a case-insensitive substring of the typed query (`type:name` prefixes included). The field does not spell-check, autocorrect, suggest, or fuzzy-match. Inventory is **resource type** (TypeIcon + tab label) then **harness section** (harness mark + name; shared `~/.agents/` uses the Agents hub mark) then wrapping **title-only** pills styled like ResourceTypeTabs. Truncate badge titles after 20 characters with `...`. Badge hover card: type, full name (wrap), path. Section-name tooltip is the location path (`~/.cursor/skills/`). Empty type sections stay. Clicking a library badge opens Library resource detail with the workspace Back. App-managed badges without a library id are not clickable. Files are never deleted from this screen.
- Saving writes the global preference only (`PUT /v1/harness { global }`) and refetches the inventory. After an add, the tracked-directory rescan runs separately; its failure toasts on its own and never rolls back the preference.
- Sticky footer under the configured list: full-width `btn primary` + `rail-apply-action` (twin of Profiles Apply). Idle **Sync harnesses** with Lucide `ArrowLeftRight` to the right of the label; busy **Syncing…** plus ButtonSpinner. Tooltip `Merge across configured harnesses. {mainName} wins conflicts.` Hidden when the list is empty. Disabled for a single configured harness (`Add another harness to sync`), with no main (`Set a main harness first`), or while a harness action is running. Confirm once: title **Sync harnesses**; body explains union merge, shared paths, and main-wins; Cancel and **Sync**. Success shows muted **Synced** under the button for about 2s; failure shows a muted error under the button and stays retryable. Calls `POST /v1/harness/sync` (same as `ht harness sync`). No Settings or header duplicate. No em dashes.

**Migrate**

- Import and Export use a quiet **Step N of M** subtitle. Step body content is a centered 640px column. Success is a toast: export includes the output path and **Reveal**; import summarizes counts.

**Lists**

- Resource hover cards open down-right of the pointer (top-left corner plus a small margin), clamp or flip to stay in the viewport, and close when the pointer leaves. Virtualized rows use `transform`, so the card is portaled to `document.body` and positioned from pointer coordinates — never `position: fixed` inside the row.
- Profile rail order is a desktop-only localStorage preference.
- Shared listbox keyboard lives in `src/lib/listbox-nav.ts`. The command palette uses it; Library / inventory / Discover / Environments adopt the same recipe.

## Privacy

On first Desktop start, before any capture, a blocking modal asks the user to enable or disable anonymous usage telemetry. The choice persists in `~/.harnesstap/config.jsonc` (`telemetry.enabled`) and is not shown again unless that preference is cleared. **Settings → Advanced → Telemetry** toggles the same preference. `HARNESSTAP_TELEMETRY=0` still disables capture as an override. Copy is high-level and matches capture: product/version/OS, install method, HarnessTap Cloud interactions, plugin install/apply/view — not personal data, plugin names, plugin contents, org ids, file paths, code, secrets, or MCP configs. Tracked / not-tracked copy uses real bullet lists. Disable is secondary; Enable is the primary dialog action.
