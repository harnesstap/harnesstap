[test]
# CLI integration tests spawn subprocesses and mock globals; allow extra time.
timeout = 30000
# Match vitest-style file isolation: avoid leaked DB/env state between test files.
isolate = true
preload = ["./test/helpers/disable-telemetry.ts"]
