kind: Fixed
body: Produce Linux AppImage, deb, and rpm desktop installers on GitHub Releases (x64 and arm64) by extracting linuxdeploy without FUSE and keeping the Bun ht-agent sidecar out of linuxdeploy's ldd scan.
