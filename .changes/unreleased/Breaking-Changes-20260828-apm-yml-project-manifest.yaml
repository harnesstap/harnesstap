kind: Breaking Changes
body: Replace `.harnesstap/config.toml` and `.harnesstap/lock.toml` with `apm.yml` and `apm.lock.yaml` as the only project manifest and lockfile. HarnessTap-only fields live under `x-harnesstap` and are honored by config, use, apply, and lock replay.
