export const MISSING_PROJECT_CONFIG_MESSAGE =
  "No project config found. Run `ht config init` to create `.harnesstap/config.toml`.";

export const PROJECT_CONFIG_EXISTS_MESSAGE =
  "Project config already exists. Pass --force to overwrite, or edit `.harnesstap/config.toml` directly.";
