export const MISSING_PROJECT_CONFIG_MESSAGE =
  "No project config found. Run `hd config init` to create `.harnessdeck/config.toml`.";

export const PROJECT_CONFIG_EXISTS_MESSAGE =
  "Project config already exists. Pass --force to overwrite, or edit `.harnessdeck/config.toml` directly.";
